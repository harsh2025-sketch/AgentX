"""Ownership boundary: ``agentx.learning``.

Canonical responsibility: research, skill compilation, reflection, repair, and
adaptive mechanisms. Learning may improve procedures over time, but it must
not grant permissions or modify its own authority boundary.

Status:

    - ``trajectory`` — deterministic C3.01 normalization of canonical C2.10
      causal experiences into ordered historical analysis trajectories.
      Normalization only: no causal-importance inference, classification,
      parameter extraction, learning, procedure synthesis, execution, routing,
      retry policy, persistence, or authority effects.
    - ``causal_actions`` — conservative C3.02 extraction of explicit recorded
      action candidates from canonical C3.01 normalized steps. Extraction keeps
      each source step unchanged and preserves ordering, duplicates, failures,
      and missing optional evidence. Candidate status does not mean causally
      necessary, relevant, useful, successful, trusted, or executable; there is
      no text heuristic, model inference, filtering, persistence, procedure
      synthesis, Hive mutation, execution, routing, retry, repair, or authority.
"""
